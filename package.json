{
  "name": "ef_lib",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "bun run --cwd web dev",
    "build": "bun run --cwd web build",
    "build:quick": "bun run --cwd web build:quick",
    "install:web": "bun install --cwd web"
  }
}
